apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: zoren-voice-prod
  labels:
    app: zoren-voice-api
    env: prod
spec:
  type: ClusterIP
  selector:
    app: zoren-voice-api
    env: prod
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
